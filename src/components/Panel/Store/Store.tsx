import styles from "@/components/Panel/Store/Store.module.scss";

const Store = () => {
    return(
        <div className={styles.store}>
            <div className={styles.store__contaner}>
                
            </div>
        </div>
    );
};

export default Store;